#!/bin/bash

if [ -z "$(git status --porcelain)" ]; then
	echo "No changes to commit."
	echo "Checked for changes at $(date)" < "./logs/$(date +'%m-%d-%Y')".txt
	exit 0
else
	git add .
	git commit -m "Modifications for $(date +'%m-%d-%Y')"
	git push origin

fi
